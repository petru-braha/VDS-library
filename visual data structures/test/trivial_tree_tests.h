#include "trivial_tree.cpp"

//------------------------------------------------
// primitives:

TEST(tree_p, initialisation)
{
	//tree<> words;
	//std::cout << words.get_node()
}


// question for the default constructor should i initialise the memory itself